{% extends 'base_login_register.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
            <tr>
                <th>Power_level</th>
                <td>{{ role.powerLevel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_role_index') }}">back to list</a>

    <a href="{{ path('app_role_edit', {'id': role.id}) }}">edit</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
